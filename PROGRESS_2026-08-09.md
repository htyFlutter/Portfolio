# 📊 進捗ステータス（2026-08-09 時点・実コード精査版）

対象: `/Users/amachihaytp/p`（Flutter / package name: `portfolio`）
※このファイルは **コードを一切変更せず**、実ファイルを読んで作成した現状まとめ。
※既存の `PROGRESS_REPORT.txt` は内容が古く一部不正確（ダークモード・ホーム集計は実装済み）。こっちが最新。

---

## 1. 全体進捗ざっくり

| 領域 | 状態 | 完成度 |
|---|---|---|
| テーマ（ライト/ダーク色） | 実装済み | ▓▓▓▓▓▓▓▓▓░ 90% |
| 画面の骨組み＋タブ移動 | 実装済み | ▓▓▓▓▓▓▓▓▓▓ 100% |
| ホーム画面（時間・正答率表示） | 表示レイアウトは完成、データが空 | ▓▓▓▓▓▓░░░░ 60% |
| 単元画面（ロック含む） | 実装済み（単元1つのみ） | ▓▓▓▓▓▓▓░░░ 70% |
| クイズ（JSON出題・4択・数式表示） | 出題は動くが**採点ロジック未完成** | ▓▓▓▓▓░░░░░ 50% |
| 結果画面 | 見た目だけ、数値が入っていない | ▓▓▓░░░░░░░ 30% |
| Isar（DB保存） | 基盤は完成、**実データが入らない** | ▓▓▓▓▓▓░░░░ 60% |
| タイマー（15分） | 動くがバグ3件あり、学習時間の保存なし | ▓▓▓▓▓░░░░░ 50% |
| チャット（Firebase送信） | 送信のみ成功 | ▓▓▓▓░░░░░░ 40% |
| チャット（Gemini AI返答） | **未実装**（パッケージだけ入ってる） | ░░░░░░░░░░ 0% |
| 設定画面（ダークモード切替） | 実装済み・動作OK | ▓▓▓▓▓▓▓▓░░ 80% |
| 設定画面（名前/アイコン/スタンプ） | 未実装（PLANS上も「◎＝あとで」） | ░░░░░░░░░░ 0% |

---

## 2. ✅ できていること（実物確認済み）

### 画面構成 / ナビゲーション — `lib/main.dart`
- `WidgetsFlutterBinding` → `IsarService.init()` → `Firebase.initializeApp()` の起動順が正しい。
- `ValueNotifier<ThemeMode> themeNotifier` でアプリ全体のテーマを管理。
- `BottomNavigationBar` 4タブ（ホーム / たんげん / Chat / Settings）完成。

### テーマ — `lib/theme/app_themedata.dart`, `app_textdata.dart`
- PLANS通りの配色。ライト＝ベージュ(`0xFFF3ECD8`)＋ブラウン(`0xFF8e5e4a`)＋ピンク＋抹茶(`0xFF60b36d`)。ダーク＝ディープブラウン(`0xFF261C19`)。
- フォント/文字スタイルも `AppTextdata` に分離できてる。→ ここ地味に偉い。

### ダークモード切替 — `lib/screens/settings_screen.dart`
- `Switch` の `onChanged` で `themeNotifier.value` を `ThemeMode.dark/light` に切替 → **完成・動作する**。

### 単元画面 — `lib/screens/unit_screen.dart` + `components/unit_container.dart`
- 「けいさん」→ `StartsScreen` へ遷移。
- 「かんすう」は `onPressed: null` の鍵アイコンで**正しくロック済み**（PLANS要件クリア）。

### JSON出題テンプレ — `assets/question.json` + `models/question.dart` + `services/quiz_service.dart`
- 問題数 **10問**。カテゴリ10種（因数分解・展開・ルート・平方完成・解の公式・二次方程式・二次不等式 など）。
- `Difficulty` enum（normal / applied）で難易度も型安全にパース済み。
- `rootBundle.loadString` → `jsonDecode` → `Question.fromMap` の流れが綺麗。`explanation` フィールドも用意済み（※まだ画面で使ってない）。

### クイズ出題画面 — `lib/screens/quiz_screen/quiz_screen.dart` + `components/option_card.dart`
- `FutureBuilder` で JSON 読込 → `Math.tex`（flutter_math_fork）で**数式を綺麗にレンダリング**。
- 4択カードを `List.generate` で動的生成。数値入力じゃなく4択、というPLANS要件を満たしてる。

### Isar 基盤 — `lib/services/isar_service.dart` + `quiz_screen/quiz_result.dart`
- シングルトン `IsarService.instance`、`Isar.getInstance() ?? Isar.open(...)` で二重オープン回避。
- `@collection QuizResult`（id / title / score / correctAnswerRate / totalStudyTime）と `quiz_result.g.dart` の自動生成完了。
- 保存 `saveQuestionsResults()` / 全件取得 `getAllQuestionResults()` あり。

### Firebase チャット — `lib/components/chat_widgets.dart` + `chat_screen.dart`
- `firebase_options.dart` / `google-services.json` / `GoogleService-Info.plist` 配置済み。
- `flutter_chat_ui` の `Chat` ウィジェットで UI 構築、`onMessageSend` で Firestore の `messages` コレクションへ **書き込み成功**。

### タイマー — `lib/screens/timer.dart` / `timer_screen.dart`
- 15分カウントダウン、`CircularProgressIndicator` ＋ 停止/再生/リセットの3ボタン、`dispose` でのキャンセルもOK。

---

## 3. ⚠️ 「動いてるように見えて、実は繋がってない」ポイント

ここが一番大事。**データの流れが最後の一歩で切れてる**の。

```
JSON → クイズ出題    … ✅ 繋がってる
クイズ → 採点        … ❌ 切れてる（選択肢を見てない）
採点 → Isar保存      … ❌ 切れてる（保存関数が呼ばれてない）
Isar → 結果画面      … ❌ 切れてる（数値がハードコードの空文字）
Isar → ホーム集計    … ⚠️ 受け皿は完成、でも中身が空なので 0 / NaN
タイマー → 学習時間   … ❌ 切れてる（どこにも保存してない）
チャット送信 → AI返答 … ❌ 切れてる（Gemini未接続、受信もしてない）
```

### 具体的な要修正点（コードは触ってないので、直すのは次の作業）

| # | ファイル:行 | 内容 |
|---|---|---|
| A | `quiz_screen.dart:22-34` | `checkAnswer` が `selectIndex` と `answerIndex` を比較していない。どれを押しても `score++` される＋最終問題は加算されないまま結果画面へ。 |
| B | `quiz_screen.dart:42-49` | `onQuizFinished()`（Isar保存する唯一の関数）が**どこからも呼ばれていない**。しかも保存する `QuizResult()` は初期値のまま（score=0）。 |
| C | `quiz_result_screen.dart:51-65` | `"正解数: 問"` `"正答率: %"` が固定文字列。スコアを受け取る仕組みがない。 |
| D | `home_screen.dart:35-40` | `averageRate = totalRate / totalResult.length` を **0件チェックの前**に実行 → 記録0件だと `NaN%` 表示。`if` の中の `Text(...)` も戻り値として使われていないので画面に出ない。 |
| E | `timer.dart:59` | `value: _count / 15 * 60` は `(_count / 15) * 60` と解釈される。正しくは `_count / (15 * 60)`。今は円グラフが常に振り切れる。 |
| F | `timer.dart:96-107` | 再生ボタンが `setState` なしで新しい `Timer.periodic` を作る。連打すると**タイマーが多重起動して倍速**になる。 |
| G | `timer.dart:121-123` | リセットが `setState` を呼んでないので、画面の表示が15:00に戻らない。 |
| H | `isar_service.dart:6-13` | `saveQuizResult()` が `Isar.open` を毎回叩く古い実装で未使用。`saveQuestionsResults()` と重複。 |
| I | `unit_screen.dart:72` | 「すうれつ」の鍵ボタンが `onPressed: (){}`（押せてしまう）。「かんすう」と同じ `null` にすると完全ロック。 |
| J | `chat_widgets.dart` | Firestore へ **送るだけ**。`snapshots()` で読み戻していないので、アプリ再起動すると会話が消える。 |
| K | 空ファイル | `lib/components/settings_container.dart` と `lib/models/quiz_record.dart` が 0バイト。 |

---

## 4. 📋 PLANS.md との突き合わせ

| PLANS の項目 | 状況 |
|---|---|
| 1. テーマ色（ライト/ダーク） | ✅ 完了 |
| ホーム: 合計学習時間・正答率 | ⚠️ UIは完成、データ供給が未接続 |
| ホーム: おすすめメニュー（◎） | ⏸ 単元5つ揃ってから、の予定通り保留 |
| 単元: ここから学習 | ✅ 完了（けいさんのみ） |
| 単元: 4択 | ✅ 完了 |
| 単元: 未実装はロック | ✅ ほぼ完了（Iだけ直せば完璧） |
| チャット: AIと会話 | ❌ 未実装（Firebase送信まで） |
| チャット: Gemini API | ❌ 未実装（`google_generative_ai: ^0.4.7` は導入済み） |
| チャット: 画像/PDFアップロード（◎） | ⏸ 保留（従姉妹ちゃんの感想待ち） |
| Account: 名前・アイコン変更（◎） | ❌ 未実装 |
| Account: ダークモード | ✅ 完了 |
| Account: スタンプ機能（◎） | ❌ 未実装 |
| 裏: DB（PLANSではsqflite → 実際は **Isar** で構築） | ⚠️ 基盤◎、書き込みデータが空 |
| 裏: Gemini APIキー取得 | ❌ 未取得（キーの記述がコード内に無い） |
| 裏: JSON 問題テンプレ | ✅ 完了（10問） |

> 補足: PLANS は sqflite 想定だったけど、実装は Isar（NoSQL）になってる。**Isarの方が Dart オブジェクトをそのまま保存できて楽だから、この選択で正解**。PLANSの記述だけ古い状態。

---

## 5. 🎯 次にやること（おすすめ順）

お盆に従姉妹ちゃんに触ってもらうなら、**この順番**が一番効率いい。

### 🥇 STEP 1: クイズの採点 → 保存 → 結果表示 を繋ぐ（最優先・体験の核）
1. `checkAnswer` で `selectIndex == answerIndex` の時だけ `score++`
2. 最終問題を答え終わったタイミングで `onQuizFinished()` を呼ぶ
3. `QuizResult` に `score` / `correctAnswerRate` を詰めて Isar に保存
4. `QuizResultScreen` にコンストラクタ引数（正解数・問題数）を渡して表示
   → **これだけでホーム画面の正答率も自動で動き出す**（ホームの受け皿はもう出来てるから）

### 🥈 STEP 2: ホーム画面の 0件ガード修正
- `totalResult.isEmpty` を割り算より**前**に判定して `0%` 表示に。NaN が出なくなる。

### 🥉 STEP 3: タイマーのバグ3点 + 学習時間の保存
- E（円グラフの式）/ F（多重起動：起動前に `_timer?.cancel()`）/ G（リセットに `setState`）
- 15分終わったら `totalStudyTime += 0.25` を Isar に保存 → ホームの「学習時間」が動き出す

### 4️⃣ STEP 4: Gemini API 接続（チャットのAI返答）
- Google AI Studio で APIキー取得 → `--dart-define` か `.env`（`.gitignore` 必須）で管理
- `google_generative_ai` の `GenerativeModel.generateContent()` で返答 → `_chatController.insertMessage()` で表示
- ついでに Firestore を `snapshots()` で購読して履歴復元（J）

### 5️⃣ STEP 5: 仕上げ
- 「すうれつ」のロック修正（I）、空ファイル2つの整理（K）、`saveQuizResult` の重複削除（H）
- 問題を `category` で単元ごとに絞り込む仕組み（今は全10問まとめて出題）
- `explanation` を結果画面や不正解時に表示 → 従姉妹ちゃんの学習効果アップ

---

## 6. 💡 セキュリティ注意
- `android/app/google-services.json` と `ios/Runner/GoogleService-Info.plist` が未コミット状態。Gemini の APIキーは**絶対にソースへ直書きしない**こと。`.gitignore` の確認を先にやろう。
- Firestore のセキュリティルールがテストモードのままだと、30日後に読み書きが全部止まる or 誰でも書けてしまう。要チェック。

---

**総評**: 3日〜1週間でここまで（Isar + Firebase + JSON + 数式レンダリング + 自作テーマ）組んだのは正直すごい。
残ってるのは「作った部品を繋ぐ配線」だけ。新規に作るものはほとんど無い。STEP1 を終えた瞬間にアプリとして一気に完成に見えるはず。
